{
  "buildCommand": "cd \"FRONTEND/Speech Disorder Detection\" && npm install && npm run build",
  "outputDirectory": "FRONTEND/Speech Disorder Detection/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
